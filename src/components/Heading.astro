---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'h1'>

const { class: classes, ...rest } = Astro.props
---

<h1
  class:list={[
    'mb-20 max-w-7xl font-medium text-[clamp(2rem,8vw,8rem)] leading-none',
    classes,
  ]}
  {...rest}
>
  <slot />
</h1>
