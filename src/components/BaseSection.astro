---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'section'>

const { class: classes, ...rest } = Astro.props as Props
---

<section
  class:list={['py-20 md:py-40', classes]}
  {...rest}
>
  <div class="container">
    <slot />
  </div>
</section>
